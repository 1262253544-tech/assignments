#include <stdio.h>
#include <stdlib.h> 
#include <time.h>  
int main() {
    srand(time(NULL));
    printf("Generating 5 pseudo-random numbers:\n");
    for (int i = 0; i < 5; i++) {
        int num = rand();
        printf("%d\n", num);
    }

    return 0;
}
